/// <reference path="IChartElement.ts" />
/// <reference path="ChartCategory.ts" />
/// <reference path="ChartElement.ts" />
/*
namespace Libraries.Charts{
    export class PieChart{
        private static clear(element:SVGElement){
            while(element.lastChild){
                element.removeChild(element.lastChild);
            }
        }

        static renderTo(element:SVGElement, item:IChartElement){
            PieChart.clear(element);
            element.classList.add("pie-chart");

            element.
        }

        readonly element:SVGElement;
        readonly children:ChartCategory;

        constructor(name:string){
            this.element = document.createElementNS("http://www.w3.org/2000/svg", "svg");
            this.element.classList.add("pie-chart");
            this.children = new ChartCategory(name);
        }

        update(){
            PieChart.renderTo(this.element, this.children);
        }
    }
}*/